# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Build scripts (not part of the public site)
*.py
scratch/
*.textClipping

# Raw, unused client phone photos - not resized/compressed, still carry EXIF/GPS.
# Not referenced anywhere on the site; kept locally only until sorted or discarded.
assets/imgs/client-photos-unsorted/

# Dependencies
node_modules/
.npm

# Env / secrets
.env
.env.*
*.key
*.pem
*.secret
